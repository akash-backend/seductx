// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  apiUrl: 'https://www.seductx.com/seductxApi/',
  firebase: {
    apiKey: 'AIzaSyDnLTWNMol1HpmL_FHf2aGfCghzboPbhzM',
    authDomain: 'angularapp-1c328.firebaseapp.com',
    databaseURL: 'https://angularapp-1c328.firebaseio.com',
    projectId: 'angularapp-1c328',
    storageBucket: 'angularapp-1c328.appspot.com',
    messagingSenderId: '616742663651'
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
